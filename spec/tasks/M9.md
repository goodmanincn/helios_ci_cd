# M9 · 插件市场 — 任务清单 (MVP 后追加)

> 工期: 4 周 (W27–W30 · 20 工作日 · 160h)
> 目标: 插件机制 + 官方插件 10 个
> 完成定义: 用户可在 UI 安装/卸载插件,流水线 `uses: helios/trivy@v1` 工作
> 前置: M2 完成 (实际不依赖 M3-M8)
> 时机: v1.0 Beta 发布后,根据用户反馈开始

---

## ✅ MVP 切片 (第一轮, 已完成)

> 单次开发轮次的产物 — 把"插件"这条主轴打通, 留好扩展点; 完整 10 插件 / 私有 registry / publish 流程待后续轮次。

**已完成**:

- `api/pkg/plugin/`: action.yml schema (`spec.go`) + 解析器 (`parse.go`) + uses ref 解析 + DB-backed Resolver (`runtime.go`) + 官方插件 seed (`seed.go`),覆盖 ParseRef / Resolver / Action 校验 10+ 个单测
- DB 三张表 (011_plugins): `plugins` (slug=namespace/name generated) / `plugin_versions` (action_yml + action_spec jsonb + is_latest) / `plugin_installations` (UNIQUE org_id+plugin_id)
- GORM model: `api/internal/model/plugin.go`
- Repository: `api/internal/repository/plugin.go` (List / GetBySlug / GetVersionByName / InstallToOrg / Uninstall / ListInstalled / IncrementDownloads)
- REST API: `api/internal/handler/plugins.go`
  - `GET    /api/v1/plugins                       ?q= ?category= ?verified=true`
  - `GET    /api/v1/plugins/installed             当前 org 已装`
  - `GET    /api/v1/plugins/:namespace/:name      详情 (含 installed + installed_version)`
  - `POST   /api/v1/plugins/:namespace/:name/install   {version}`
  - `DELETE /api/v1/plugins/:namespace/:name/install`
- 8 个 handler 集成测试 (PG tx, 跟 host_groups_test 同范式)
- 3 个官方种子插件: `helios/echo@v1` / `helios/trivy@v1` / `helios/dingtalk@v1` — 启动自动 seed, slug 冲突跳过
- Worker 执行路径 (`api/pkg/runengine/execute.go`):
  - `step.Uses` 命中 builtin → 走 builtin 注册表
  - 未命中 → `plugin.Resolver.Resolve(orgID, ref)` → 容器执行 (`docker run` shell out, 把 `INPUT_*` 注入 env, 挂 workspace 到 `/github/workspace`, 解析 stdout `::set-output name=X::value` 协议)
  - composite 解析通过但 runtime 返显式 "not yet supported"
- main.go 接线:
  - api: `plugin.SeedOfficial(gdb)` 启动时跑一次 + `handler.NewPluginHandler(gdb).Register(protected)`
  - worker: `handler.NewStageExecute(...).WithPluginResolver(plugin.NewSQLResolver(db))`
- 前端 (dark theme):
  - `web/src/app/plugins/page.tsx` — 卡片网格 + 搜索 + 分类筛选 + verified 徽章
  - `web/src/app/plugins/[namespace]/[name]/page.tsx` — README + inputs/outputs 表 + 版本下拉 + 安装/卸载/切版本
  - `web/src/app/settings/plugins/page.tsx` — 已安装管理 + 卸载 (二次确认) + 升级提示
  - `web/src/lib/marketplace.ts` — API client
  - sidebar 菜单"插件"启用 (`enabled: true`)

**显式 deferred** (本轮没做, 留后续轮次):
- 远程 registry 协议 + CDN 拉取 (T9.2.2 / T9.2.3 / T9.2.5) — 当前只 seed + 内嵌
- 完整 10 个官方插件 (T9.4.4-T9.4.10) — 当前 3 个
- helios CLI `plugin init/dev/publish` (T9.3.4)
- composite step 递归真正执行 (T9.1.4) — 解析通过, runtime 报"not yet supported"
- 镜像签名验证 cosign (T9.1.3 子项)
- 评分 + 评论 (T9.3.5)
- 版本约束求解 (`^1.2.0` / `~1.2.0`) (T9.2.4 子项) — 当前只支持 `@vN` / `@x.y.z` / `@latest` + 第三方 `github.com/foo/bar@v1` 留口子但拒
- needs_permissions 强制 sandbox (T9.1.5) — 当前仅声明并展示给用户

**验证通过**:
- `go build ./...` × api + worker
- `go test ./...` × api + worker (含 8 个 plugin handler 集成 + 2 个 resolver 集成 + 7 个 parser 单测)
- 前端 ESLint clean (plugins / settings 新增页)

---

## E9.1 action.yml 契约 + 容器化执行器 (5d)

### T9.1.1 action.yml 规范 (1d)
**步骤**:
- `pkg/plugin/spec.go` 完整 schema
- 字段: name / description / inputs / outputs / runs (container/composite/javascript)
- 校验 + 文档示例
- container 子: image / args / env / entrypoint
- composite 子: steps (复合 step)

**验收**: 10 个示例 action.yml 都能 parse 通过

---

### T9.1.2 inputs/outputs 接口 (1d)
**步骤**:
- inputs 通过环境变量传 (INPUT_XXX)
- outputs 通过 stdout 协议 (`::set-output name=X::value`)
- 文件系统约定 (workspace / artifacts / runner-tmp)
- 兼容 GitHub Actions 大部分协议

**验收**: 一个 echo 插件能输出值给下游

---

### T9.1.3 容器化执行器 (1.5d)
**步骤**:
- 复用 Docker runner (M1)
- 镜像拉取策略 (always/IfNotPresent/Never)
- 镜像签名验证 (cosign,可选)
- 资源限制 (cpu/mem 默认值)
- 安全沙箱 (no-new-privileges, 只读 rootfs 可选)

**验收**: 跑 trivy 插件能扫镜像

---

### T9.1.4 composite step 支持 (1d)
**步骤**:
- 解析 composite 中的 sub-steps
- 子 step 复用引擎递归执行
- 嵌套 secrets / vars 传递
- 防无限递归 (深度限制 5)

**验收**: 一个 composite 插件 (含 3 个 sub-step) 工作

---

### T9.1.5 插件级 secrets / permissions (0.5d)
**步骤**:
- action.yml 声明 needs_secrets
- action.yml 声明 needs_permissions (network/fs/exec)
- 执行时强制
- 用户安装时显示权限请求

**验收**: 越权调用被拦截

---

## E9.2 插件 registry (4d)

### T9.2.1 registry 数据模型 (0.5d)
**步骤**:
- plugins 表: id / name / namespace / description / publisher / repository / verified
- plugin_versions 表: plugin_id / version / action_yml / readme / changelog
- plugin_installations 表: org_id / plugin_id / version / installed_at

**验收**: 模型 + 迁移

---

### T9.2.2 官方 registry 协议 (1d)
**步骤**:
- 索引文件: registry.helios.io/index.json
- 插件 manifest: registry.helios.io/plugins/{namespace}/{name}/{version}/action.yml
- 签名: GPG 或 cosign
- 镜像源: 国内镜像 + CDN

**验收**: 拉取 index 成功

---

### T9.2.3 私有 registry 支持 (1d)
**步骤**:
- 多 registry 配置 (org 级)
- 认证 (token / basic auth)
- 优先级 + 镜像备选
- 离线缓存

**验收**: 私有 GitLab 仓库的 action 能跑

---

### T9.2.4 引用解析 (uses: ...) (1d)
**步骤**:
- 解析 `helios/trivy@v1` / `helios/trivy@1.2.3` / `helios/trivy@latest`
- 版本约束 (^1.2.0 / ~1.2.0)
- 第三方仓库: `github.com/foo/bar@v1`
- 本地路径: `./local-action`

**验收**: 4 种引用形式都能解析

---

### T9.2.5 缓存与预热 (0.5d)
**步骤**:
- 已安装插件预拉镜像
- 卸载时垃圾回收
- 缓存命中率指标

**验收**: 重复使用插件秒级启动

---

## E9.3 插件市场 UI + 安装/版本管理 (4d)

### T9.3.1 插件市场列表页 (1d)
**步骤**:
- /marketplace 路由
- 卡片网格 (logo / name / description / 下载量 / 评分 / verified 徽章)
- 分类筛选 + 搜索
- 排序 (热门 / 最新 / 评分)

**验收**: 视觉一致

---

### T9.3.2 插件详情页 (1d)
**步骤**:
- /marketplace/:namespace/:name
- README 渲染 + Markdown
- inputs / outputs 表格
- 版本下拉 + changelog
- 安装按钮 (版本选择)
- 权限请求弹窗

**验收**: 信息齐全

---

### T9.3.3 已安装管理 (0.5d)
**步骤**:
- /settings/plugins
- 已安装列表 + 版本 + 升级按钮
- 卸载 (二次确认)
- 升级历史

**验收**: 升级流畅

---

### T9.3.4 发布插件流程 (1d)
**步骤**:
- 开发者文档
- helios CLI: helios plugin init / dev / publish
- 自动校验 action.yml
- 自动构建镜像 + 推送 + 注册

**验收**: 一个新插件能从代码到上架 < 30 分钟

---

### T9.3.5 评分 + 评论 (0.5d)
**步骤**:
- 安装过的用户可评分 (1-5 星)
- 评论 + 举报
- 平均分显示

**验收**: 基本社交功能

---

## E9.4 官方插件首批 10 个 (7d)

### T9.4.1 Trivy 镜像扫描 (0.5d)
**步骤**:
- inputs: image / severity / format
- outputs: vulnerabilities_count
- 镜像: aquasec/trivy

**验收**: 扫 alpine:latest 报漏洞

---

### T9.4.2 SonarQube 代码扫描 (0.5d)
**步骤**:
- inputs: project_key / token / server
- outputs: quality_gate_status

**验收**: 扫 demo Go 项目

---

### T9.4.3 钉钉通知 (0.5d)
**步骤**:
- inputs: webhook / secret / message / at_mobiles
- 模板化消息 (markdown)

**验收**: 实际群收到消息

---

### T9.4.4 邮件 (0.5d)
**步骤**:
- inputs: to / subject / body / smtp_*

**验收**: 收到邮件

---

### T9.4.5 Slack (0.5d)
**步骤**:
- inputs: webhook / message / channel
- 支持 attachments / blocks

**验收**: Slack 收到

---

### T9.4.6 Helm Release (1d)
**步骤**:
- inputs: cluster / namespace / release / chart / values
- helm upgrade --install --wait
- output: release_name / revision

**验收**: 部署一个 chart

---

### T9.4.7 Kaniko 构建 (1d)
**步骤**:
- inputs: dockerfile / context / destination / build_args
- 无 docker daemon 构建
- 推送到 registry

**验收**: 构建并推送一个镜像

---

### T9.4.8 Buildpacks (1d)
**步骤**:
- inputs: builder / app / image
- pack build
- 无 Dockerfile

**验收**: Spring Boot demo 构建成功

---

### T9.4.9 Jest 测试 (0.5d)
**步骤**:
- inputs: working_dir / config
- 覆盖率报告 → artifact
- 失败时输出测试结果

**验收**: 跑一个 React 项目测试

---

### T9.4.10 Pytest 测试 (0.5d)
**步骤**:
- inputs: working_dir / config / coverage
- pytest --cov 报告 → artifact
- JUnit XML 输出

**验收**: 跑一个 Django 项目测试

---

### T9.4.11 插件文档 + 示例仓库 (0.5d)
**步骤**:
- 每插件单独 README
- examples/ 目录每插件一份 demo pipeline yaml
- 文档站插件分类

**验收**: 用户能照做

---

## M9 整体验收清单

- [ ] 插件市场列表 10 个官方插件
- [ ] uses: helios/trivy@v1 流水线 YAML 能跑
- [ ] 用户能发布一个第三方插件
- [ ] 私有 registry 支持
- [ ] composite 插件能用
- [ ] 卸载/升级/降级流畅
- [ ] 缓存命中率 > 90%

## 风险

- **action.yml 协议设计变更成本高** — 上线前 review 慎重,留 escape hatch
- **GitHub Actions 兼容性** — 不追求 100%,只做声明式部分,Composite/JS Action 后置
- **官方插件维护成本** — 接受社区贡献,官方只保 10 个核心
- **registry 高可用** — CDN + 多镜像源
- **总计 20d ≈ 4w,无缓冲**
