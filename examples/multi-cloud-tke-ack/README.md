# 多云 TKE + ACK 示例

对应模板 `multi-cloud-tke-ack`。

## 准备

- 平台已注册集群 `prod-tke`、`prod-ack`
- `REGISTRY` / `IMAGE_NAME` 与镜像仓库一致

```bash
helios templates clone multi-cloud-tke-ack -p 1 -n multi-deploy
```
