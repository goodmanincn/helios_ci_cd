# Go 多平台 + GitHub Release 示例

对应模板 `go-multi-platform`。入口 `./cmd/app`。

## Secrets

- `GITHUB_TOKEN` — `gh release upload` 用

## 克隆

```bash
helios templates clone go-multi-platform -p 1 -n go-release
```
